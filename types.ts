export type Line = {
  /**
   * Unique ID of the line. Used as URL.
   */
  id: string
  /**
   * Human readable name of the line.
   */
  name: string
  /**
   * Line description.
   */
  description: string
}
